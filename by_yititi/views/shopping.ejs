<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BEE - Shopping Cart</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="fixed-header">
        <h1>Bee 🐝</h1>
        <ul class="menu">
            <li><a href="/home">首頁</a></li>
            <li><a href="/login">登入</a></li>
            <li><a href="/products">產品介紹</a></li>
            <li><a href="/service">服務項目</a></li>
            <li><a href="/shopping">購物車</a></li>
            <li><a href="/contact">聯絡我們</a></li>
        </ul>
    </div>
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>
    <h1>Bee 🐝</h1>
    <h2>Lab303 2024</h2>
    <p id="Introduce">Welcome to Bee, sharing some information about bees here :D<br>Shopping cart</p>
    <div class="shopping-cart-container">
        <h2>購物車</h2>

        <div class="cart-items">
            </div>

        <div class="cart-summary">
            <p>小計：$<span id="subtotal">0.00</span></p>
            <p>運費：$<span id="shipping">0.00</span></p>
            <p>總計：$<span id="total">0.00</span></p>

            <button onclick="checkout()">結帳</button>
        </div>
    </div>

    <script>
        // Function to calculate and update cart totals
        function updateTotals() {
            let subtotal = 0;
            for (const item of cartItems) {
                subtotal += item.price * item.quantity;
            }

            const shipping = subtotal > 50 ? 0 : 5;
            const total = subtotal + shipping;

            document.getElementById('subtotal').textContent = subtotal.toFixed(2);
            document.getElementById('shipping').textContent = shipping.toFixed(2);
            document.getElementById('total').textContent = total.toFixed(2);
        }

        // Function to handle checkout
        function checkout() {
            // Implement checkout logic here, such as redirecting to a checkout page
            alert('結帳功能尚未實作');
        }
    </script>
</body>
</html>
